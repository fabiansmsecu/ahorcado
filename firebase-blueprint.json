{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Stores user data including their score",
      "type": "object",
      "properties": {
        "score": {
          "type": "number",
          "description": "Total score earned in the game"
        },
        "name": {
          "type": "string",
          "description": "Display name of the player"
        },
        "uid": {
          "type": "string",
          "description": "ID of the user, immutable after creation"
        }
      },
      "required": ["score", "name", "uid"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "UserProfile",
      "description": "Collection of user profiles containing their scores"
    }
  }
}
